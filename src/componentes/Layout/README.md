Qué es un Layout en React?

Un layout es un componente envoltorio que sirve para repetir una estructura común en varias páginas. Por ejemplo:

El Navbar (barra de navegación).

El Footer (pie de página).

Un sidebar, fondo o contenedor general.

Y un espacio donde se cargan las rutas (contenido que cambia según la URL).

📌 ¿Por qué usar un layout?

Porque te permite:

Escribir el navbar una sola vez y que se vea en muchas rutas.

No mostrarlo en páginas específicas (como login/register).

Mantener tu código más limpio y organizado.